// This is a placeholder. Use the native MongoDB driver in your route files.
// Example usage in routes: const db = require('../utils/database').getDb();
// Then use db.collection('users') for queries.

module.exports = {};
